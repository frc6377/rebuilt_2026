package frc.robot.subsystems.example;

import edu.wpi.first.wpilibj2.command.SubsystemBase;

public class exampleSubsystem extends SubsystemBase {
    /*Each subsystem has its own designated file which contains the subsystem and the subsystems constants
     * a subsystem should contain only a specific process and should be easy to quarantine if something
     * goes wrong. For example the shooter should be in a seperate subsystem from the turret so that if
     * the turret malfunctions we can easily turn it off.
     */
}
