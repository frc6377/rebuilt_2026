I will analyze why the shooting mechanism fails in simulation and develop a fix.

### Technical Plan: Fix Simulation Shooting

1. **Analyze Simulation Specifics:**
   - Inspect `src/main/java/frc/robot/subsystems/vision/GamePieceCameraSim.java` and any shooter/indexer simulation logic to see how game pieces are handled in the virtual world.
   - Check if the `Indexer` or `Shooter` subsystems have `simulationPeriodic()` methods that require explicit state updates for simulation.

2. **Verify Physics/Sensor Feedback in Sim:**
   - Check if the shooter flywheels ever reach "target velocity" in simulation. If the simulation model for the motors is missing or incorrect, the `Indexer` might be waiting forever for a `atSetpoint()` signal that never comes.
   - Inspect `src/main/java/frc/robot/subsystems/shooter/BaseShooter.java` for its simulation implementation (e.g., `FlywheelSim`).

3. **Check Indexer Simulation:**
   - Verify if the `Indexer` uses digital sensors (beams-breaks) to detect balls. These often need to be manually "tripped" in simulation or updated via a simulation model to allow the transition to shooting.

4. **Implement Simulation Logic Fixes:**
   - If missing, add `FlywheelSim` to `BaseShooter`.
   - Ensure `atSetpoint()` logic accounts for simulation or has a bypass if the physics model is lagging.
   - Update `Indexer` simulation to properly "exhaust" balls when the shoot command is active.

5. **Validation:**
   - Confirm the trigger logic in `RobotContainer` is correctly passing commands to the simulation-wrapped subsystems.

---

### Step 1: Examine RobotContainer and Subsystem Simulation Implementations
I'll start by looking at how the shooter and indexer are initialized and if they have simulation-specific logic.

/read "src/main/java/frc/robot/RobotContainer.java"
/grep "simulationPeriodic" "src/main/java/frc/robot/subsystems/"
/ls "src/main/java/frc/robot/subsystems/shooter"
/ls "src/main/java/frc/robot/subsystems/indexer"
