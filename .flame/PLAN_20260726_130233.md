To implement active shooting functionality while holding the shoot button, I will follow this technical plan:

### Technical Plan: Implement Continuous Shooting

1.  **Analyze Command Binding:**
    *   Read `src/main/java/frc/robot/RobotContainer.java` to identify where the "shoot" button is bound and what command it currently triggers.
2.  **Inspect Subsystem State Machines:**
    *   Examine `src/main/java/frc/robot/subsystems/shooter/BaseShooter.java` to verify the `runShooter` or equivalent method logic.
    *   Examine `src/main/java/frc/robot/subsystems/indexer/Indexer.java` to determine how it feeds the shooter.
3.  **Verify Command Logic:**
    *   Locate the specific command (e.g., `ShootCommand` or an inline `RunCommand`) to ensure it invokes both the Shooter's flywheels and the Indexer's feed mechanism.
4.  **Modify/Fix Logic:**
    *   If the command is currently a `Trigger.onTrue()`, change it to `Trigger.whileTrue()`.
    *   Ensure the Indexer only activates when the Shooter flywheels are at the target RPM (velocity check).
5.  **Validation:**
    *   Review `src/main/java/frc/robot/subsystems/upgoer/Upgoer.java` if it's involved in the vertical transport of balls to the shooter.

---

### Step 1: Analyze RobotContainer and Bindings
I'll start by checking the current implementation of the shoot binding.

/grep "shoot" "src/main/java/frc/robot/RobotContainer.java"
/read "src/main/java/frc/robot/RobotContainer.java"
